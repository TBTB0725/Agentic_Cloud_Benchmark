# Local Buggy Fixture

This is a minimal local fixture repository for the ACBench prototype.

It exists to validate the code benchmark path without depending on external repositories or containers.

The repository contains:

- a small Python module with a seeded bug
- a unittest suite
- a gold patch stored outside the fixture for repair validation
