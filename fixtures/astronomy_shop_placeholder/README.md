This directory is a local placeholder fixture for astronomy-shop-shaped prototype scenarios.

It exists so ACBench example scenarios can remain self-contained during migration away from
direct sibling-repository app paths in external reference repositories.

These placeholder scenarios are illustrative shape examples, not the primary stable demo path.
